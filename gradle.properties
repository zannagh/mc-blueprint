org.gradle.jvmargs=-Xmx4G
org.gradle.parallel=true
org.gradle.configureondemand=true
org.gradle.configuration-cache=false

loom.shareRemapCaches=true

# Mod identity (maven_group, archives_base_name), the Fabric loader_version, and the
# per-version java.version / display_version / minecraft version ranges all live in
# stonecutter.properties.toml, where Stonecutter injects them per variant.
