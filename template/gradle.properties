org.gradle.jvmargs=-Xmx4G
org.gradle.parallel=true
org.gradle.configureondemand=true
org.gradle.configuration-cache=false

# Properties set per-version in branch version directories (common/versions/*, fabric/versions/*, ...):
# java.version, display_version, fabric.minecraft_version, fabric.minecraft_version_range,
# neoforge.version, neoforge.minecraft_version_range

# Loom and loader
loom_version=1.14-SNAPSHOT
loader_version=0.18.2

# Mod Properties
maven_group=com.example.examplemod
archives_base_name=example-mod
